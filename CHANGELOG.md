# 서울 교통·생활 정보 — 변경 이력

> 배포와 의미있는 수정만 기록한다. 오타·리팩터링·되돌린 커밋은 적지 않는다.
> 최신이 위. 날짜는 절대표기(YYYY-MM-DD).
> 작업 맥락·금지사항은 [PROJECT_STATUS.md](PROJECT_STATUS.md) 참고.
>
> 이 파일은 **2026-08-12에 처음 만들었다.** 그 이전 이력(2026-06 공공의료 앱 → 지하철 전용 개편
> → 생활서비스 확장 → data.go.kr 대량 확장 → 탭 정리·야간진료 추가)은 **`git log`에만** 있다.
> 소급 정리는 하지 않았다 — 필요하면 `git log --oneline`으로 본다.
> 배포는 `vercel --prod --yes` **CLI 수동**이라 push가 곧 배포는 아니다.

## 2026-08-12 (4) — 스냅샷 자동 갱신 · 깨져 있던 주차장 빌드 복구 · 공단 실시간 접음

### 🅿️ `build:parking` 이 깨져 있었다 (발견 → 복구)
공단 건을 진행하려고 돌려보다 **표준데이터 빌드 자체가 죽어 있는 걸 발견**했다.
data.go.kr 이 JSON 봉투를 바꿨다 — `{response:{header,body}}` → **`{header,body}`**,
`body.items` 도 배열 → `{item:[…]}`. 옛 경로만 보던 탓에 `header` 가 undefined 가 되어
`page 1 실패: undefined undefined` 로 죽었다. **둘 다 받도록** 고쳤다(되돌릴 수도 있으니).
- 결과: 전국 주차장 **17,768 → 18,117곳**(원본 18,879건 중 좌표 있는 것), 스냅샷 3.74MB.
- 교훈: 스냅샷을 오래 안 구우면 **원본 API 변경을 알아챌 방법이 없다.** 아래 자동화의 근거.

### ⏱️ 스냅샷 자동 갱신 (`.github/workflows/refresh-snapshots.yml`)
`data/*.js` 는 원본에 위치 필터가 없어 통째로 구운 것이라, 화면의 진료시간·요금은
**구운 시점 기준**이다. 손으로만 굽고 있었다 → 매월 1일(+수동) 자동 리빌드.
- 주차장·야간진료·영업소를 각각 `continue-on-error` 로 굽고, **문법 검사 통과 시에만** 커밋.
- ⚠️ 이 저장소는 **Vercel 이 Git 에 안 붙어 있다**(CLI 수동 배포) → 커밋만으로는 운영 미반영.
  `VERCEL_TOKEN` 시크릿이 있으면 배포까지, 없으면 경고만 남긴다.
- 빌드 스크립트 2개가 **`process.env` 가 아니라 `.env` 파일을 직접 읽고 있어** CI 에서
  동작 불가였다(키는 커밋 금지라 CI 에 `.env` 가 없다) → 환경변수 우선 + `.env` 폴백으로 수정.
- **테스트 실행에서 고속도로 빌드가 CI 를 10분+ 잡아먹는 걸 발견** → 월간 대상에서 뺐다.
  영업소 목록은 거의 불변인데 `realUnitTrtm` OD 쌍을 수백 페이지 훑어야 해서다. `build:tollgates`
  는 수동 전용으로 남기고, 스크립트에 **조기 종료(saturated)·시간 예산·안전 저장**(잘리면
  기존 566곳을 덮지 않음)을 넣었다. 자동 갱신은 실제로 낡는 **주차장·야간진료**만.
- 키 없으면 **실패가 아니라 조용히 건너뛰기**로(월간 실패 알림 메일 방지).
- **검증(2026-08-13)**: 수동 실행으로 파이프라인 확인 — 봇이 `e101cea`로 야간진료 갱신분을
  자동 커밋까지 성공. **CI 자동배포는 뺐다** — `vercel --prod --token` 이 무효 토큰에 "User not
  found"로 실패했고, org/project ID 까지 맞물려야 하는 취약한 고리라 CI 에 두지 않는다.
  스냅샷은 CI 가 갱신·커밋만, 운영 반영은 세션에서 `vercel --prod --yes`(사용자 방침 "배포는 너가해").
- 필요한 시크릿: `DATA_API_KEY` 하나(넣으면 자동 갱신, 없어도 조용히 건너뜀). EX_API_KEY·VERCEL_TOKEN 은 안 씀.

### 🚗 공단 실시간 주차면수 — 접었다
백엔드는 **회복했다**(`diag` 세 오퍼레이션 모두 응답). 그런데 진짜 문제는 규모였다.
- 시설 `totalCount` **1,767,934** · 1,000건/페이지 × ~0.6초 → 전량 1,400페이지+, **15분·수십 MB**.
- **서버측 필터 없음** — 시도·시군구·ID 파라미터를 붙여도 `totalCount` 불변(전부 무시).
- 표준데이터의 `prkplceNo` 와 `prk_center_id` 는 다른 체계라 join 불가 → 실시간만 받아도 좌표가 없다.
- ⇒ 「회복 대기」가 아니라 **구조적 한계**로 §10 에 옮겼다. 서울 실시간 잔여면수는 이미
  서울 열린데이터로 동작 중이라 실사용 손실은 작다. `KOTSA_PARKING` 은 계속 off.

### 📱 모바일 실기기 확인 완료
사용자 확인 — 560px 이하 히어로 가로형·2열 그리드 정상. 세 세션 걸쳐 남아 있던 항목 종료.

## 2026-08-12 (3) — 리뉴얼이 어긋나게 만든 주변 3건 정리

홈 허브를 올린 뒤 «이 정도면 괜찮나» 점검에서 나온 3건. 화면이 아니라 **화면 밖 약속**이
안 맞는 것들이었다.

### 이용가이드가 없어진 UI를 설명하고 있었다 (`guide.html`)
헤더에서 한 번에 닿는 페이지인데 `"교통 카테고리의 📍 내주변 탭에서…"`처럼 **따라 할 수 없는
안내**가 남아 있었다. 내가 UI를 바꿔서 생긴 문제.
- **화면 구성** 섹션 전면 교체 — "상단 5개 서비스 탭" → 홈 카드 + `← 홈` sticky 설명,
  표도 **지금 켜져 있는 8개**로(내주변·야간진료·주유소·따릉이·고속도로 추가, 실거래가·분실물 삭제).
- **🏥 야간진료 가이드 신설** — 2026-08-06에 탭만 추가되고 가이드엔 설명이 아예 없었다.
  종류·야간기준·반경 필터 + **"진료시간은 신고값이라 가기 전 전화 확인" 경고**.
- 상단 브랜드·푸터·히어로 문구의 **옛 앱 이름(「서울 지하철 정보 찾기」)** 정리.
- 즐겨찾기·지도·GPS 설명의 탭 목록을 실제와 일치시킴. 카테고리 표현 → "화면"으로.
- 내려둔 기능(분실물·실거래가 랭킹)은 **지우지 않고** "현재 메뉴에서 내려둠" 배지 + 경고 박스.
  사이드바 목차의 죽은 링크만 제거(가리키는 곳은 남아 있으니 앵커는 안 깨진다).
- `수정된 사항`에 08-12·08-06 카드 추가(NEW 배지 이동).

### 히어로 카드 4개 중 2개에 ⭐즐겨찾기·🔗공유가 없었다 (`js/favorites.js`)
`PANELS` 레지스트리에 `clinic`·`nearby`가 **아예 없었다**. 고속도로엔 있는데 야간진료엔 없는
상태. 리뉴얼 전엔 묻혀 있어 안 보였는데 홈에서 큰 카드로 내세우니 드러났다.
- 두 패널 등록 + `PANEL_LABEL`(모아보기 그룹명)에 추가.
- clinic 의 select 4개는 **`changeFields` 로 두지 않았다** — 변경 리스너가 캐시 유무로
  가드돼 있어 값만 세팅하면 되고, `clRadius` 에 change 를 쏘면 `searchClinic()` → GPS 재요청이 된다.
- 실측: 저장 → 라벨 `치과 · 오후 9시 이후 · 2km` → 홈 «모아보기»에 `🏥 야간진료` 그룹으로 복원.

### title·og 가 없는 기능을 광고하고 있었다 (`index.html`)
`실거래가·미세먼지·시내버스·LH청약`은 전부 `data-off`인데 검색 문구에 그대로 있었다
(탭 6개 숨긴 세션에서 같이 안 고쳐진 것). 살아 있는 8개 기준으로 재작성.
**탭을 다시 켜면 이 문구도 같이 되돌려야 한다** — 주석으로 박아 뒀다.

## 2026-08-12 (2) — 상단 군더더기 제거(헤더 제목 · 카테고리 바 · 서브탭 줄)

홈 허브를 올리고 나니 **헤더의 「서울 교통·생활 정보」와 홈 브랜드 블록이 같은 말을 두 번**
하고 있었고, 모바일에서 제목이 3줄로 접혀 첫 화면을 절반 가까이 먹었다. 사용자 요청으로 정리.

- **헤더**: 큰 제목·소개문구 삭제, 버튼줄(미세먼지 배지·앱 설치·테마·이용가이드)만 남긴
  `.app-header.slim`. 워터마크도 같이 축소(얇아진 헤더에서 잘린 조각만 보이지 않게).
  ⚠️ `<h1>`은 **지우지 않고 `.sr-only`** — 검색엔진·스크린리더가 읽을 제목이 사라지면 안 된다.
- **카테고리 바(홈·교통·주거·생활) 삭제 · 서브탭 줄은 화면에서만 제거.**
  ⚠️ `.toptab` 버튼은 DOM에 그대로 둔다 — 홈 카드의 원본 목록이자 `data-off` 계약의 근거라
  지우면 홈이 빈다. `showCategory()`·`firstTabOfCat()`도 미사용이지만 남겼다.
- **대체 내비**: 탭 안에서 `← 홈 | 🏥 야간진료` 한 줄. 결과가 길어도 닿게 **sticky**
  (`.topnav.nav-bar`, 홈에서는 클래스가 빠져 높이 0). 화면 이름은 숨은 `.toptab` 텍스트를
  그대로 읽어 이름의 원본을 하나로 유지 — 딥링크로 바로 들어와도 어디인지 알 수 있다.
- 효과(실측, 첫 화면): 헤더 **161→68px**, 본문 시작 **245→96px**. 데스크톱에서 홈 전체가
  한 화면에 들어온다.

## 2026-08-12 — 🏠 홈 허브 신설(첫 화면 리뉴얼)

첫 화면이 곧 지하철 탭이었다. 나머지 기능은 카테고리(교통·주거·생활)를 먼저 눌러야 보여서
**「이 앱에 무엇이 있는지」가 첫 화면에 없었다** — 주차장 하나 열려면 [주거]→[주차장] 2클릭.
약보듬(`yakbodeum`)의 홈 허브 구조를 참고해 **살아 있는 탭 전부를 1클릭**으로 폈다.

### 홈 허브
- `#home` 패널 신설 — 브랜드 → "무엇을 찾으세요?" → **자주 찾는 정보 4칸**(지하철·내주변·주차장·
  야간진료, 설명 딸린 큰 카드) → **전체 서비스 그리드 8칸** → 즐겨찾기 모아보기·이용가이드.
- **카드는 HTML에 박지 않았다.** `renderHub()`가 `index.html`의 `.toptab:not([data-off])`에서
  생성 → 탭을 숨기면 홈 카드도 사라지고, `data-off`만 지우면 카드까지 함께 돌아온다.
  설명·대표색은 `js/services.js`의 `HUB` 표(`hero:1`이면 위쪽 큰 카드에도).
- 내비에 `🏠 홈` 칩 추가(`data-cat="__home"` 센티널 — 어떤 `.toptab`과도 안 맞아 다른
  카테고리로 가면 `showCategory()`가 알아서 끈다). 주거 칩 아이콘은 🏠 중복이라 🏘️로.
- 카드 색은 `--hue` 하나만 넘기고 표면은 `color-mix(…, var(--card))`로 만든다 → 다크 테마에서
  자동으로 어두운 틴트(하드코딩 파스텔이면 다크에서 흰 카드가 튄다).
- 딥링크·공유링크는 그대로(`#gas` 등으로 들어오면 홈을 건너뛴다). 해시가 없을 때만 홈.

### 같이 잡은 것
- **`.subtabs[hidden]{display:none}`** — 작성자 스타일 `display:flex`가 UA의 `[hidden]`을 이겨서
  홈인데 서브탭 줄이 그대로 보였다. JS의 `el.hidden`만 읽으면 감춰진 줄 오판한다(육안에서 발견).
- **노선도 폭 보정** — 홈이 첫 화면이 되며 노선도가 숨은 채 그려져 `clientWidth` 0 → `baseW`가
  최소값 320px로 굳었다. `initMapZoom`이 `window.__refitSubwayMap`을 노출하고
  `switchPanel("subway")`이 호출(+`resize` 리스너, 재렌더 시 옛 리스너 제거). 실측 457px.
- **헤더 미세먼지 배지** — 미세먼지 탭이 `data-off`라 눌러도 갈 곳이 없었다. 탭이 꺼져 있으면
  클릭 핸들러를 달지 않고 커서도 기본으로(눌러도 아무 일 없는 배지 제거).
- `sw.js` `VERSION` v5→v6(옛 셸 캐시 폐기).

### 검증
로컬 dev-server(3021) + Chrome: 홈→주차장/지하철 이동·홈 복귀·`#gas` 딥링크·서브탭 접힘
(`getComputedStyle` = `none`)·라이트/다크 카드 틴트·콘솔 에러 0.
⚠️ **데스크톱 2열 히어로는 실측 못 함** — 이 머신 Chrome 확장이 뷰포트를 501px 위로 못 늘린다.
560px 이하 모바일(1열 가로형 카드 + 2열 그리드)만 육안 확인.

## 2026-08-12 (앞) — 문서 커밋 누락 정리

직전 세션이 코드는 커밋·push·배포까지 마쳤으나 갱신한 `PROJECT_STATUS.md`를 커밋하지 않고
종료해, 문서가 "working tree 깨끗"이라 적은 채 자기 자신이 미커밋으로 남아 있었다(214+/219−).
재작성분을 독립 커밋으로 확정하고 §0 자기참조 문장을 코드 기준 서술로 정정했다. 코드 변경 없음.
